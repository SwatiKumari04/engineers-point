{
  "name": "engineers-point",
  "version": "2.0.0",
  "private": true,
  "description": "Night-canteen ordering platform for NIT Jamshedpur with student and owner panels and concurrency-safe flash sales.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "start": "npm start --workspace server",
    "test": "npm test --workspace server",
    "lint": "npm run lint --workspace client",
    "build": "npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=22"
  }
}
